标识符是用来给变更、类、方法以及包进行命名。标识符需要遵循的规则如下：
1、标识符必须以字母、下划线、美元符号$开头；
2、标识符其它部分可以是字母、数字、下划线"_"、美元符号"$"任意组合；
3、java标识符大小写敏感且长度无限制；
4、标识符不可以是java关键字。
注：java不采用通常语言使用的ASCII字符集，而是采用Unicode这样的标准国际字符集。因此字母的含义不仅仅是英文还包括汉字等等。

标识符使用规范：
1、表示类名的标识符：每个单词首字母大写。
2、表示变量和方法的标识符：第一个单词首字母小写，从第二个单词开始首字母大写。